#lang racket

(define b 3)

; this closure is lexically scoped -- it will always take the value of b to be 3
(define f (lambda (x) (*1 (+ (x b)))))

(define c (+ b 4))
(set! b 5)
(define z (f 4))
(define w c)
